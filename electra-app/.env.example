# Copy this file to .env and adjust as needed

# Local development (backend running on host)
DATABASE_URL=postgresql://electra:electra@localhost:5432/electra

# If backend runs inside Docker network, use the service name as host
# DATABASE_URL=postgresql://electra:electra@db:5432/electra

# Note: SQLAlchemy-style URLs like postgresql+psycopg://... are normalized automatically.
